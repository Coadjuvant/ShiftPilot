ShiftPilot Output Patch

Fixes:
- Coverage sheet pivot fix (no more single 'Tech 4' filled).
- Roster shows time spans.
- New WeekMatrix sheet (People x Mon..Sun, grouped by role).

Apply:
Unzip over Backend_Seed/ so it overwrites scheduler_app/io/export_v2.py
Re-run the generator or API.
